package at.qe.skeleton.model;


import org.junit.jupiter.api.Test;
import java.time.LocalDate;
import static org.junit.jupiter.api.Assertions.assertEquals;


class PlantTest {
/*
    private final Plant plant = new Plant();

    @Test
    void testPlantID() {
        plant.setPlantID(123L);
        assertEquals(123L, (long) plant.getPlantID());
    }

    @Test
    void testPlantName() {
        plant.setPlantName("Test Plant");
        assertEquals("Test Plant", plant.getPlantName());
    }

    @Test
    void testCreateDate() {
        LocalDate now = LocalDate.now();
        plant.setCreateDate(now);
        assertEquals(now, plant.getCreateDate());
    }

    @Test
    void testUpdateDate() {
        LocalDate now = LocalDate.now();
        plant.setUpdateDate(now);
        assertEquals(now, plant.getUpdateDate());
    }

    @Test
    void testDescription() {
        plant.setDescription("Test plant description");
        assertEquals("Test plant description", plant.getDescription());
    }

*/

}
